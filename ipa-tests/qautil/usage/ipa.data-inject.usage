-----------------------------------------------------------------
USAGE: 
	ipa.data-inject <server> <data size>

EXAMPLE:
	ipa.data-inject ipaserver.rhqa.net small

<data size>
	small =  total 10 groups, 100 users, 5 layers,                        |"
		 2 groups per layer, 10 users each group
		 no cycling group 
	middle= total 150 groups, 150,000 users 5 layers, 
		 30 groups each layer, 1000 users each group, 
		 no cycling group" 
	large = 1,000 groups, 2,000,000 users, 100 layers, 
		10 groups each layer, 2000 user each group,
		random group-user relation" 
-----------------------------------------------------------------
