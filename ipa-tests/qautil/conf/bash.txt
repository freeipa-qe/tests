# test machine env setup
alias vi='vim'
if [ -d /root/ipa-tests/trunk/ipa-tests ]; then
        IPA_TESTS=/root/ipa-tests/trunk/ipa-tests
        PATH=$PATH:$IPA_TESTS/qautil
        export PATH IPA_TESTS
fi
PS1="\h[\D{%D %R}]\w> "
export PS1

