# version : 1.0
# IPA server 
ipahost    :	ipa52.test.com
admindn    :	cn=directory manager
adminpasswd:	redhat123
base       :	cn=accounts,dc=test,dc=com
usr_suffix :	cn=users,cn=accounts,dc=test,dc=com
grp_suffix :	cn=groups,cn=accounts,dc=test,dc=com
    
#test data size
gsize : 5
glevel: 4
# the following will be computed by program automaticly 
#	usize = $gsize * 10; 
#	ulevel = $glevel;
#	baseuid_index = $usize * $ulevel * 200;
#	basegid_index = $gsize * $glevel * 100;  
#	maxloop = $usize * $ulevel * 10;

# weight of each operation/type
# notes: add+mod+del should be 100
add_weight : 15
mod_weight : 70
del_weight : 15

# grp + usr should be 100
grp_weight : 10
usr_weight : 90
