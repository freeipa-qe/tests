Initial Prep/Setup:
##.  Setup IPA MASTER
##.  Setup NIS MASTER
##.  Setup NIS CLIENT1
##.  Setup NIS CLIENT2

IPA MASTER CMD tests:
##.  Run IPA MASTER Netgroup CLI CMD tests
##.  Run IPA MASTER Automount CLI CMD tests
##.  Run IPA MASTER Service CLI CMD tests
##.  Cleanup all CMD test entries left behind

IPA MASTER NIS Map Integration:
##.  Add NIS good info to IPA using CLI
##.  FAIL to add NIS bad info to IPA using CLI
##.  Delete NIS good info from IPA
##.  FAIL to delete NIS bad info from IPA using CLI
##.  Add NIS good info to IPA using ldif
##.  FAIL to add NIS bad info to IPA using ldif 
##.  Enable NIS Listener
##.  Find good info with ypcat
##.  FAIL to find bad info with ypcat

NIS CLIENT Migration:
##.  Lookup NIS good info directly from IPA MASTER
##.  FAIL to Lookup NIS bad info directly from IPA MASTER
##.  Point NIS config to IPA MASTER
##.  Lookup NIS good info using new default config

NIS Netgroup Additional Tests:
##.  Test netgroup hbac for accessing NIS MASTER with ssh?
##.  FAIL netgroup hbac for denying IPA MASTER with ssh
##.  Test netgroup user access for allowed sudo
##.  FAIL Test netgroup user access for disallowed sudo
##.  Test different host netgroup type entries?
##.  Test different user netgroup type entries?
##.  Test different mixed netgroup type entries?

NIS Automount Additional Tests:
##.  Test mount of NFS export from NIS MASTER using automount

NIS Services Additional Tests:
##.  Add service map entry for a new port
##.  Run test service using portname?  netcat/python/etc?

NIS CLIENT CMD tests:
##.  Prep by setting up default NIS info on IPA MASTER
##.  Run NIS CLIENT Netgroup CLI CMD tests
##.  Run NIS CLIENT Automount CLI CMD tests
##.  Run NIS CLIENT Service CLI CMD tests
##.  Run NIS SSSD config tests

IPA MASTER BZ tests:
##.  Run IPA MASTER Netgroup CLI BZ tests
##.  Run IPA MASTER Automount CLI BZ tests
##.  Run IPA MASTER Service CLI BZ tests

NIS CLIENT BZ tests:
##.  Run NIS CLIENT Netgroup CLI BZ tests
##.  Run NIS CLIENT Automount CLI BZ tests
##.  Run NIS CLIENT Service CLI BZ tests

OTHER NIS related BZ tests:
##.  Run other BZ tests
