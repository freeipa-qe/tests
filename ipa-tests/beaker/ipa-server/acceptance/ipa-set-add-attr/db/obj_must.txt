MUST ( cn $ nsSaslMapRegexString $ nsSaslMapBaseDNTemplate $ nsSaslMapFilterTemplate )
MUST ( objectclass $ nsServerID )
MUST ( cn $ oncRpcNumber )
MUST ( nsRoleDN )
MUST ( cn $ vlvSort )
MUST ( CN )
MUST (cosattribute)
MUST ( objectclass )
MUST ( ipHostNumber $ cn )
MUST ( javaClassName )
MUST ( cn $ nisMapEntry $ nisMapName )
MUST ( nsDS5ReplicaRoot $  nsDS5ReplicaId )
MUST ( objectClass $ cn )
MUST ( cn $ uid $ uidNumber $ gidNumber $ homeDirectory )
MUST ( ntUserDomainId )
MUST ( cn $ ipServicePort $ ipServiceProtocol )
MUST ( javaSerializedData )
MUST ( cn $ gidNumber )
MUST ( ipNetworkNumber $ cn )
MUST ( cn $ ipProtocolNumber )
MUST ( cn $ vlvBase $ vlvScope $ vlvFilter )
MUST ( nisMapName )
MUST ( cn $ nsSystemIndex )
MUST ( cn $ nsEncryptionAlgorithm )
MUST ( targetdn $ changeTime $ changenumber $ changeType )
MUST ( nsRoleFilter )
MUST ( cn $ nsSNMPEnabled )
MUST ( cn $ nsServerID )
MUST ( cn )
