#!/usr/bin/expect

set username [lindex $argv 0]
set password [lindex $argv 1]

set timeout 5
set send_slow {1 .1}
spawn ipa passwd $username
expect "New Password: "
send -s -- "$password\r"

expect "Enter New Password again to verify: "
send -s -- "$password\r"

expect eof
