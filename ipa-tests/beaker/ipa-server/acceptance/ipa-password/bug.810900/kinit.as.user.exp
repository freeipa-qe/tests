#!/usr/bin/expect

set username [lindex $argv 0]
set password [lindex $argv 1]

set timeout 5
set send_slow {1 .1}

spawn /usr/bin/kinit $username
expect "Password for $username*: "
send -s -- "$password\r"

expect eof

